substitutions:
  name: switch007
  description: Basement Table Fan

wifi:
  ssid: !secret iot_wifi
  password: !secret iot_wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Switch007 Fallback Hotspot"
    password: "W3jOhSCNQSSU"

captive_portal:

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:

<<: !include include/sonoff_mini_fan.yaml
